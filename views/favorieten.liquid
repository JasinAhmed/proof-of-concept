{% render 'partials/head.liquid' %}
{% render 'partials/header.liquid' %}

<section class="favorites-page">
    <div class="favorites-container">

        <nav class="breadcrumb">
            <img src="/style/assets/house@2x.svg" alt="" class="icon">
            / Mijn account / Favorieten
        </nav>

        <h1>{{ title }}</h1>
        <div class="title-line"></div>

        {% if houses.size == 0 %}

            <div class="empty-state">
                <div class="empty-icon">
                    <img src="/style/assets/heart@2x.svg" alt="Favorieten leeg">
                </div>

                <h2>Tip: bewaar een huis</h2>

                <p>
                    Tik op het hart-icoon bij je favoriete huis om deze toe te voegen aan je favorieten.
                </p>

                <a href="/" class="search-button">
                    Start met zoeken
                </a>
            </div>

        {% else %}

            <div class="houses-grid">

                {% for house in houses %}

                    <a href="/huis-detail/{{ house.id }}" class="house-card">

                        <img
                            src="https://fdnd-agency.directus.app/assets/{{ house.poster_image.id }}?width=600"
                            alt="{{ house.street }} {{ house.house_nr }}"
                            loading="lazy"
                            width="600"
                            height="400"
                        >

                        <div class="house-card-content">

                            <h2>
                                {{ house.street }}
                                {{ house.house_nr }}
                                {{ house.nr_addition }}
                            </h2>

                            <p>
                                {{ house.postal_code }}
                                {{ house.city }}
                            </p>

                            <strong>
                                € {{ house.price }}
                            </strong>

                            <div class="house-features">

                                <span>
                                    <img src="/style/assets/bed@2x.svg" alt="" class="icon">
                                    {{ house.rooms }} kamers
                                </span>

                                <span>
                                    <img src="/style/assets/icons/area.svg" alt="" class="icon">
                                    {{ house.m2 }} m²
                                </span>

                            </div>

                        </div>

                    </a>

                {% endfor %}

            </div>

        {% endif %}

    </div>
</section>

{% render 'partials/footer.liquid' %}